name: "Eternal Keypad Firmware"

on:
  workflow_dispatch:
  push:
    branches:
    - main
    paths:
    - .github/workflows/eternal_keypad.yml
    - eternal_keypad/**

jobs:
  eternal_keypad-default-helios:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: keebd/build-qmk-fw@main
        with:
          keyboard: eternal_keypad
          keymap: default
          controller: helios
          output-dir: build-output
      - uses: actions/upload-artifact@v4
        with:
          name: eternal_keypad-default-helios
          path: build-output/*.uf2
          if-no-files-found: error

  eternal_keypad-via-helios:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: keebd/build-qmk-fw@main
        with:
          keyboard: eternal_keypad
          keymap: via
          controller: helios
          output-dir: build-output
          local-keymap-path: eternal_keypad/keymaps/via
      - uses: actions/upload-artifact@v4
        with:
          name: eternal_keypad-via-helios
          path: build-output/*.uf2
          if-no-files-found: error

  eternal_keypad-default-svlinky:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: keebd/build-qmk-fw@main
        with:
          keyboard: eternal_keypad
          keymap: default
          controller: svlinky
          output-dir: build-output
      - uses: actions/upload-artifact@v4
        with:
          name: eternal_keypad-default-svlinky
          path: build-output/*.uf2
          if-no-files-found: error

  eternal_keypad-via-svlinky:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: keebd/build-qmk-fw@main
        with:
          keyboard: eternal_keypad
          keymap: via
          controller: svlinky
          output-dir: build-output
          local-keymap-path: eternal_keypad/keymaps/via
      - uses: actions/upload-artifact@v4
        with:
          name: eternal_keypad-via-svlinky
          path: build-output/*.uf2
          if-no-files-found: error

  eternal_keypad-release:
    needs:
      - eternal_keypad-default-helios
      - eternal_keypad-via-helios
      - eternal_keypad-default-svlinky
      - eternal_keypad-via-svlinky
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: firmware
          merge-multiple: true
      - name: Publish firmware as a rolling release
        # Pinned to a commit SHA (supply-chain hardening); Renovate bumps the
        # SHA + comment via PR. This action runs with contents: write.
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        with:
          tag_name: eternal_keypad-firmware
          name: Eternal Keypad Firmware (latest)
          body: |
            Latest Eternal Keypad Firmware, built automatically from `main`.

            Download the `.uf2` matching your keymap and controller, then flash it.
          files: firmware/*.uf2
