name: "Yampad Firmware"

on:
  workflow_dispatch:
  push:
    branches:
    - main
    paths:
    - .github/workflows/yampad.yml
    - yampad/**

jobs:
  yampad-default-helios:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: keebd/build-qmk-fw@main
        with:
          keyboard: yampad
          keymap: default
          controller: helios
          output-dir: build-output
      - uses: actions/upload-artifact@v4
        with:
          name: yampad-default-helios
          path: build-output/*.uf2
          if-no-files-found: error

  yampad-via-helios:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: keebd/build-qmk-fw@main
        with:
          keyboard: yampad
          keymap: via
          controller: helios
          output-dir: build-output
          local-keymap-path: yampad/keymaps/via
      - uses: actions/upload-artifact@v4
        with:
          name: yampad-via-helios
          path: build-output/*.uf2
          if-no-files-found: error

  yampad-default-svlinky:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: keebd/build-qmk-fw@main
        with:
          keyboard: yampad
          keymap: default
          controller: svlinky
          output-dir: build-output
      - uses: actions/upload-artifact@v4
        with:
          name: yampad-default-svlinky
          path: build-output/*.uf2
          if-no-files-found: error

  yampad-via-svlinky:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: keebd/build-qmk-fw@main
        with:
          keyboard: yampad
          keymap: via
          controller: svlinky
          output-dir: build-output
          local-keymap-path: yampad/keymaps/via
      - uses: actions/upload-artifact@v4
        with:
          name: yampad-via-svlinky
          path: build-output/*.uf2
          if-no-files-found: error

  yampad-release:
    needs:
      - yampad-default-helios
      - yampad-via-helios
      - yampad-default-svlinky
      - yampad-via-svlinky
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: firmware
          merge-multiple: true
      - name: Publish firmware as a rolling release
        # Pinned to a commit SHA (supply-chain hardening); Renovate bumps the
        # SHA + comment via PR. This action runs with contents: write.
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        with:
          tag_name: yampad-firmware
          name: Yampad Firmware (latest)
          body: |
            Latest Yampad Firmware, built automatically from `main`.

            Download the `.uf2` matching your keymap and controller, then flash it.
          files: firmware/*.uf2
